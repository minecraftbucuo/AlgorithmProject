题目描述：
给定一个长度为 $n$ 的数组，你需要计算它的哈基值。哈基值的计算方法如下：从左到右遍历数组中第一个数到倒数第二个数，如果这个数的右边存在严格大于它的数，则将其乘以 $-1$ ，否则保持不变。操作完后，这个数组中所有数的和就是其哈基值。

输入描述：
第一行一个整数 $n \space (1 \le n \le 10^5)$ 。
第二行 $n$ 个整数 $a_1, a_2, \dots, a_n$ ，$(-10^6 \le a_i \le 10^6)$ 。

输出描述：
输出一个整数，表示计算得到的哈基值。

